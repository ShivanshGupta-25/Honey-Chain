npm install express mongoose dotenv cors helmet morgan express-rate-limit
npm install -D nodemon
npm install bcryptjs
npm install jsonwebtoken

# mkdir config, controllers, middleware, models, routes, services, utils
# New-Item .env, .env.example, .gitignore, server.js -ItemType File